repos:
-   repo: local
    hooks:
    - id: ruff-format
      name: Ruff format
      entry: uv run ruff format
      language: system
      types: [python]
      args: ["--"]
    - id: ruff-check
      name: Ruff lint
      entry: uv run ruff check
      language: system
      types: [python]
      args: ["--"]
    - id: ty
      name: Ty type check
      entry: uv run ty check
      language: system
      pass_filenames: false
    - id: pytest
      name: Pytest non-heavy
      entry: uv run pytest
      language: system
      pass_filenames: false
      args: ["-m", "not heavy", "--maxfail=1", "--disable-warnings"]
